interface:
  display_name: "ProductForge"
  short_description: "Intent to use-case-closed increments"
  default_prompt: "Use $productforge to turn this intent into a use-case-closed product increment with the right MVP artifact, contracts, implementation plan, and evidence."

policy:
  allow_implicit_invocation: true
