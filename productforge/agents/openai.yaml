interface:
  display_name: "ProductForge"
  short_description: "PRD, SDD contracts, plans, tasks"
  default_prompt: "Use $productforge to turn this product idea into a PRD, SDD contracts, a technical plan, and implementation tasks."

policy:
  allow_implicit_invocation: true
