interface:
  display_name: "ProductForge"
  short_description: "Intent to verifiable product increments"
  default_prompt: "Use $productforge to turn this intent into a scene-first product increment with contracts, implementation plan, and evidence."

policy:
  allow_implicit_invocation: true
